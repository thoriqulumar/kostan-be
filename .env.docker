# Docker Compose Environment Variables
# Copy this to .env and update with your production values

# Database
DB_PASSWORD=your-strong-password-here

# JWT Secret (Generate using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=your-generated-jwt-secret-here

# CORS - Update with your domain
CORS_ORIGIN=http://localhost,https://yourdomain.com,https://www.yourdomain.com

# Frontend API URL - Update with your domain
VITE_API_URL=http://localhost:3000
# Or for production with domain:
# VITE_API_URL=https://yourdomain.com
